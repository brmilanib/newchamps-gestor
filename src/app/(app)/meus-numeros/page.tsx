import { EmBreve } from "@/components/ui";

export default function MeusNumerosPage() {
  return (
    <EmBreve titulo="Meus Números" fase="Fase 2">
      BI só do seu dado: curva ABC, cobertura de estoque, rentabilidade por canal e fechamento
      mensal — com evolução mês a mês.
    </EmBreve>
  );
}
