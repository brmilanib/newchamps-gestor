import { EmBreve } from "@/components/ui";

export default function EstoquePage() {
  return (
    <EmBreve titulo="Estoque & Gestor Seller" fase="Fase 1 (próxima parte)">
      Upload do estoque do dia, Top 10 e ruptura com sugestão de compra, e geração do
      import__4_.xlsx pronto pro Gestor Seller.
    </EmBreve>
  );
}
