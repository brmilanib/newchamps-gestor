import { EmBreve } from "@/components/ui";

export default function MuralPage() {
  return (
    <EmBreve titulo="Mural (Modo TV)" fase="Fase 2">
      Tela cheia, só de leitura, pra deixar na TV do escritório: alertas ao vivo, mensagens dos
      grupos, o que os agentes estão fazendo e as vendas do dia.
    </EmBreve>
  );
}
