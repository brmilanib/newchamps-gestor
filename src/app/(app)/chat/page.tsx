import { EmBreve } from "@/components/ui";

export default function ChatPage() {
  return (
    <EmBreve titulo="Chat com o Supervisor" fase="Fase 3">
      Você vai perguntar em português e o Supervisor consulta o dado real e responde citando a
      fonte. Também será exposto como API por token.
    </EmBreve>
  );
}
