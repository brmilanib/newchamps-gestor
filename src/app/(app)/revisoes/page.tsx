import { EmBreve } from "@/components/ui";

export default function RevisoesPage() {
  return (
    <EmBreve titulo="Revisões semanais" fase="Fase 3b">
      Todo domingo o Supervisor presta contas: o que mudou, o que os agentes entregaram, onde
      erraram e as 3 prioridades da semana.
    </EmBreve>
  );
}
