import { EmBreve } from "@/components/ui";

export default function AlertasPage() {
  return (
    <EmBreve titulo="Alertas" fase="Fase 4">
      Tipos configuráveis, destinatário e canal por pessoa, com anti-fadiga (agrupa, respeita
      silêncio e escala em vez de repetir).
    </EmBreve>
  );
}
