import { EmBreve } from "@/components/ui";

export default function ConfiguracoesPage() {
  return (
    <EmBreve titulo="Configurações" fase="Fase 1 (próxima parte)">
      Critérios de negócio editáveis, contexto da empresa, lojas, usuários (cadastro da equipe),
      conexões e IAs conectadas.
    </EmBreve>
  );
}
